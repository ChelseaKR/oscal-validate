# Which of NIST's published constraints this tool evaluates

Generated from the vendored metaschema files for OSCAL 1.2.3
(retrieved 2026-08-14) by `tools/constraint_coverage.py`. Do not edit by hand:
`make coverage-doc` regenerates it and `tests/test_constraint_coverage.py`
fails if it is stale.

This file exists because "no findings" and "every published constraint passed"
are different claims, and only the first one is ever true here. Every
constraint NIST publishes is listed below with whether this tool runs it, and
where it does not, the reason.

## Summary

113 of 340 published constraints are evaluated.

| Constraint kind | Published | Evaluated |
|---|---:|---:|
| `allowed-values` | 200 | 0 |
| `expect` | 12 | 0 |
| `has-cardinality` | 11 | 11 |
| `index` | 20 | 19 |
| `index-has-key` | 24 | 24 |
| `is-unique` | 48 | 48 |
| `matches` | 25 | 11 |
| **total** | **340** | **113** |

## Evaluated

| Constraint | Kind | Level | Declared on | Target |
|---|---|---|---|---|
| `oscal-activity-type-cardinality` | has-cardinality | ERROR | `activity` | `prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='method']` |
| `oscal-assesment-part-objective-cardinality` | has-cardinality | ERROR | `part` | `.[@name='objective']/prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='method']` |
| `oscal-assessment-method-cardinality` | has-cardinality | ERROR | `local-objective` | `part[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name=('assessment','assessment-method')]/prop[has-oscal-namespace(('http://csrc.nist.gov/ns/oscal','http://csrc.nist.gov/ns/rmf')) and @name='method']` |
| `oscal-assessment-method-id-cardinality` | has-cardinality | ERROR | `local-objective` | `part[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name=('objective','assessment-objective')]/prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='method-id']` |
| `oscal-assessment-objective-cardinality` | has-cardinality | ERROR | `local-objective` | `part[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name=('objective','assessment-objective')]` |
| `oscal-assessment-objects-cardinality` | has-cardinality | ERROR | `local-objective` | `part[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name=('assessment','assessment-method')]/part[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name=('objects','assessment-objects')]` |
| `oscal-back-matter-resource-base64-rlink-cardinality` | has-cardinality | WARNING | `resource` | `rlink|base64` |
| `oscal-by-component-export-provided-responsibility-cardinality` | has-cardinality | ERROR | `export` | `provided|responsibility` |
| `oscal-implemented-requirement-by-component-cardinality` | has-cardinality | ERROR | `implemented-requirement` | `.//by-component` |
| `oscal-metadata-location-address-cardinality` | has-cardinality | WARNING | `location` | `address` |
| `oscal-metadata-location-title-address-email-address-telephone-cardinality` | has-cardinality | ERROR | `location` | `title|address|email-address|telephone-number` |
| `oscal-back-matter-resource-uuid-index` | index | ERROR | `back-matter` | `resource` |
| `oscal-by-component-export-provided-uuid-index` | index | ERROR | `control-implementation` | `implemented-requirement//by-component/export/provided` |
| `oscal-catalog-controls` | index | ERROR | `catalog` | `//control` |
| `oscal-catalog-groups` | index | ERROR | `catalog` | `//group` |
| `oscal-catalog-groups-controls-parts` | index | ERROR | `catalog` | `//(control|group|part)` |
| `oscal-catalog-params` | index | ERROR | `catalog` | `//param` |
| `oscal-catalog-parts` | index | ERROR | `catalog` | `//part` |
| `oscal-catalog-props` | index | ERROR | `catalog` | `//prop` |
| `oscal-index-metadata-location-uuid` | index | ERROR | `metadata` | `location` |
| `oscal-index-metadata-party-organizations-uuid` | index | ERROR | `metadata` | `party[@type='organization']` |
| `oscal-index-metadata-party-uuid` | index | ERROR | `metadata` | `party` |
| `oscal-index-metadata-property-uuid` | index | ERROR | `metadata` | `.//prop` |
| `oscal-index-metadata-role-id` | index | ERROR | `metadata` | `role` |
| `oscal-index-metadata-roles` | index | ERROR | `metadata` | `role` |
| `oscal-index-system-component-uuid` | index | ERROR | `component-definition` | `component` |
| `oscal-index-system-implementation-component-uuid-service` | index | ERROR | `system-implementation` | `component[@type='service']` |
| `oscal-system-implementation-component-leveraged-authorization-uuid-index` | index | ERROR | `system-implementation` | `leveraged-authorization` |
| `oscal-system-implementation-component-uuid-index` | index | ERROR | `system-implementation` | `component` |
| `oscal-system-implementation-component-validation-uuid-index` | index | ERROR | `system-implementation` | `component[@type='validation']` |
| `oscal-by-component-export-provided-uuid-index` | index-has-key | ERROR | `export` | `responsibility` |
| `oscal-by-component-uuid-index` | index-has-key | ERROR | `by-component` | `link[@rel='provided-by']` |
| `oscal-catalog-groups-controls-parts` | index-has-key | WARNING | `control` | `link[@rel=('related','required','incorporated-into','moved-to') and starts-with(@href,'#')]` |
| `oscal-component-prop-physical-location` | index-has-key | ERROR | `system-component` | `prop[@name='physical-location']` |
| `oscal-diagram-index-back-matter-resource-link-rel` | index-has-key | ERROR | `diagram` | `link[@rel='diagram' and starts-with(@href,'#')]` |
| `oscal-implemented-requirement-index-metadata-party-uuid` | index-has-key | ERROR | `implemented-requirement` | `responsible-role[party-uuid]|statement/responsible-role[party-uuid]|.//by-component//responsible-role[party-uuid]` |
| `oscal-implemented-requirement-index-metadata-role-id` | index-has-key | ERROR | `implemented-requirement` | `responsible-role|statement/responsible-role|.//by-component//responsible-role` |
| `oscal-index-inventory-item-responsible-party-party-uuid` | index-has-key | ERROR | `inventory-item` | `responsible-party` |
| `oscal-index-inventory-item-responsible-party-role-id` | index-has-key | ERROR | `inventory-item` | `responsible-party` |
| `oscal-index-metadata-location-uuid` | index-has-key | ERROR | `location-uuid` | `.` |
| `oscal-index-metadata-location-uuid` | index-has-key | ERROR | `defined-component` | `prop[@name='physical-location']` |
| `oscal-index-metadata-party-organizations-uuid` | index-has-key | ERROR | `member-of-organization` | `.` |
| `oscal-index-metadata-party-uuid` | index-has-key | ERROR | `party-uuid` | `.` |
| `oscal-index-metadata-role-id` | index-has-key | ERROR | `role-id` | `.` |
| `oscal-index-system-implementation-component-uuid-service` | index-has-key | ERROR | `system-implementation` | `component/link[@rel='uses-service']` |
| `oscal-leveraged-authorization-index-back-matter-resource-ssp` | index-has-key | ERROR | `leveraged-authorization` | `link[@rel='system-security-plan' and starts-with(@href,'#')]` |
| `oscal-metadata-action-name-index-metadata-party-uuid` | index-has-key | ERROR | `action` | `responsible-party` |
| `oscal-metadata-action-name-index-metadata-role-id` | index-has-key | ERROR | `action` | `responsible-party` |
| `oscal-metadata-link-reference-index-back-matter-resource` | index-has-key | ERROR | `link` | `.[@rel=('reference') and starts-with(@href,'#')]` |
| `oscal-metadata-responsible-party-index-metadata-role-id` | index-has-key | ERROR | `responsible-party` | `.` |
| `oscal-system-implementation-component-depends-on-link-index` | index-has-key | ERROR | `system-implementation` | `component/link[@rel='depends-on']` |
| `oscal-system-implementation-component-prop-leveraged-authorization-uuid-index` | index-has-key | ERROR | `system-implementation` | `component/prop[@name='leveraged-authorization-uuid']` |
| `oscal-system-implementation-validation-index` | index-has-key | ERROR | `system-implementation` | `component/link[@rel='validation' and starts-with(@href,'#')]` |
| `oscal-system-information-index-back-matter-resource-pia-link-rel` | index-has-key | ERROR | `system-information` | `link[@rel='privacy-impact-assessment' and starts-with(@href,'#')]` |
| `oscal-metadata-unique-document-id` | is-unique | ERROR | `metadata` | `document-id` |
| `oscal-unique-activity-responsible-role` | is-unique | ERROR | `activity` | `responsible-role` |
| `oscal-unique-ap-local-definitions-component` | is-unique | ERROR | `local-definitions` | `component` |
| `oscal-unique-ap-local-definitions-user` | is-unique | ERROR | `local-definitions` | `user` |
| `oscal-unique-ar-attestation-responsible-party` | is-unique | ERROR | `attestation` | `responsible-party` |
| `oscal-unique-ar-local-definitions-component` | is-unique | ERROR | `local-definitions` | `component` |
| `oscal-unique-ar-local-definitions-user` | is-unique | ERROR | `local-definitions` | `user` |
| `oscal-unique-associated-activity-responsible-role` | is-unique | ERROR | `associated-activity` | `responsible-role` |
| `oscal-unique-component-definition-capability` | is-unique | ERROR | `component-definition` | `capability` |
| `oscal-unique-component-definition-capability-incorporates-component` | is-unique | ERROR | `capability` | `incorporates-component` |
| `oscal-unique-component-definition-control-implementation-set-parameter` | is-unique | ERROR | `control-implementation` | `set-parameter` |
| `oscal-unique-component-definition-implemented-requirement-responsible-role` | is-unique | ERROR | `implemented-requirement` | `responsible-role` |
| `oscal-unique-component-definition-implemented-requirement-set-parameter` | is-unique | ERROR | `implemented-requirement` | `set-parameter` |
| `oscal-unique-component-definition-implemented-requirement-statement` | is-unique | ERROR | `implemented-requirement` | `statement` |
| `oscal-unique-component-definition-statement-responsible-role` | is-unique | ERROR | `statement` | `responsible-role` |
| `oscal-unique-defined-component-responsible-role` | is-unique | ERROR | `defined-component` | `responsible-role` |
| `oscal-unique-implemented-component-responsible-party` | is-unique | ERROR | `implemented-component` | `responsible-party` |
| `oscal-unique-inherited-responsible-role` | is-unique | ERROR | `inherited` | `responsible-role` |
| `oscal-unique-inventory-item-responsible-party` | is-unique | ERROR | `inventory-item` | `responsible-party` |
| `oscal-unique-metadata-doc-id` | is-unique | ERROR | `metadata` | `document-id` |
| `oscal-unique-metadata-link` | is-unique | WARNING | `metadata` | `link` |
| `oscal-unique-metadata-property` | is-unique | ERROR | `metadata` | `prop` |
| `oscal-unique-metadata-responsible-party` | is-unique | ERROR | `metadata` | `responsible-party` |
| `oscal-unique-poam-local-definitions-component` | is-unique | ERROR | `local-definitions` | `component` |
| `oscal-unique-profile-modify-set-parameter` | is-unique | ERROR | `modify` | `set-parameter` |
| `oscal-unique-provided-responsible-role` | is-unique | ERROR | `provided` | `responsible-role` |
| `oscal-unique-resource-base64-filename` | is-unique | ERROR | `resource` | `base64` |
| `oscal-unique-resource-rlink-href` | is-unique | ERROR | `resource` | `rlink` |
| `oscal-unique-responsibility-responsible-role` | is-unique | ERROR | `responsibility` | `responsible-role` |
| `oscal-unique-satisfied-responsible-role` | is-unique | ERROR | `satisfied` | `responsible-role` |
| `oscal-unique-ssp-assessment-assets-component` | is-unique | ERROR | `assessment-assets` | `component` |
| `oscal-unique-ssp-authorization-boundary-diagram` | is-unique | ERROR | `authorization-boundary` | `diagram` |
| `oscal-unique-ssp-by-component-set-parameter` | is-unique | ERROR | `by-component` | `set-parameter` |
| `oscal-unique-ssp-control-implementation-set-parameter` | is-unique | ERROR | `control-implementation` | `set-parameter` |
| `oscal-unique-ssp-data-flow-diagram` | is-unique | ERROR | `data-flow` | `diagram` |
| `oscal-unique-ssp-implemented-requirement-by-component` | is-unique | ERROR | `implemented-requirement` | `by-component` |
| `oscal-unique-ssp-implemented-requirement-responsible-role` | is-unique | ERROR | `implemented-requirement` | `responsible-role` |
| `oscal-unique-ssp-implemented-requirement-set-parameter` | is-unique | ERROR | `implemented-requirement` | `set-parameter` |
| `oscal-unique-ssp-implemented-requirement-statement` | is-unique | ERROR | `implemented-requirement` | `statement` |
| `oscal-unique-ssp-implemented-requirement-statement-by-component` | is-unique | ERROR | `statement` | `by-component` |
| `oscal-unique-ssp-network-architecture-diagram` | is-unique | ERROR | `network-architecture` | `diagram` |
| `oscal-unique-ssp-related-task-responsible-party` | is-unique | ERROR | `related-task` | `responsible-party` |
| `oscal-unique-ssp-statement-responsible-role` | is-unique | ERROR | `statement` | `responsible-role` |
| `oscal-unique-ssp-system-characteristics-responsible-party` | is-unique | ERROR | `system-characteristics` | `responsible-party` |
| `oscal-unique-ssp-system-implementation-user` | is-unique | ERROR | `system-implementation` | `user` |
| `oscal-unique-ssp-uses-component-responsible-party` | is-unique | ERROR | `uses-component` | `responsible-party` |
| `oscal-unique-step-responsible-role` | is-unique | ERROR | `step` | `responsible-role` |
| `oscal-unique-system-component-responsible-role` | is-unique | ERROR | `system-component` | `responsible-role` |
| `oscal-back-matter-resource-prop-published-datatype` | matches | ERROR | `resource` | `prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='published']/@value` |
| `oscal-check-hash-length-SHA2-3-224` | matches | ERROR | `hash` | `.[@algorithm=('SHA-224','SHA3-224')]` |
| `oscal-check-hash-length-SHA2-3-256` | matches | ERROR | `hash` | `.[@algorithm=('SHA-256','SHA3-256')]` |
| `oscal-check-hash-length-SHA2-3-384` | matches | ERROR | `hash` | `.[@algorithm=('SHA-384','SHA3-384')]` |
| `oscal-check-hash-length-SHA2-3-512` | matches | ERROR | `hash` | `.[@algorithm=('SHA-512','SHA3-512')]` |
| `oscal-component-inherited-uuid-value-datatype` | matches | ERROR | `system-component` | `prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='inherited-uuid']/@value` |
| `oscal-component-inherited-uuid-value-datatype` | matches | ERROR | `defined-component` | `prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='inherited-uuid']/@value` |
| `oscal-component-prop-uri-value-datatype` | matches | ERROR | `system-component` | `prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='uri']/@value` |
| `oscal-component-release-date-value-datatype` | matches | ERROR | `system-component` | `prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='release-date']/@value` |
| `oscal-component-release-date-value-datatype` | matches | ERROR | `defined-component` | `prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='release-date']/@value` |
| `oscal-metadata-telephone-number-regex` | matches | WARNING | `telephone-number` | `.` |

## Evaluated, but reading an index that is never built

These constraints are parsed and run, and they can never produce a definite answer: the `index` constraint that would populate the index they read is one of the skipped constraints below, so every lookup misses. References checked against them are reported UNVERIFIABLE, naming the index, and are never reported as failures of the document.

| Constraint | Declared on | Reads index | Populated by |
|---|---|---|---|
| `oscal-by-component-uuid-index` | `by-component` | `by-component-uuid` | `oscal-ssp-by-component-uuid-index`, skipped |

## Not evaluated

Neither passed nor failed. A document that this tool reports no findings for may still violate any of these.

| Constraint | Kind | Declared on | Why not |
|---|---|---|---|
| `(unnamed)` | allowed-values | `mapping-item` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `mapping-resource-reference` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `qualifier-item` | it is declared on the flag subject, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `qualifier-item` | it is declared on the flag predicate, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `qualifier-item` | it is declared on the flag category, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `-` | it is declared on the flag matching-rationale, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `-` | it is declared on the flag status, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `-` | it is declared on the flag method, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `-` | it is declared on the flag match-with-child-controls, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `(unnamed)` | allowed-values | `relationship` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `(unnamed)` | allowed-values | `category` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-activity-type-values` | allowed-values | `activity` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-activity-values` | allowed-values | `activity` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-alter-position-values` | allowed-values | `add` | it is declared on the flag position, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-assesment-part-objective-method-value` | allowed-values | `part` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-assesment-part-objective-name` | allowed-values | `part` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-assessment-objective-types` | allowed-values | `local-objective` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-assessment-part-values` | allowed-values | `part` | it is declared on the flag name, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-assessment-subject-type-values` | allowed-values | `-` | it is declared on the flag subject-type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-assessment-subject-values` | allowed-values | `assessment-subject` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-back-matter-resource-hash-algorithm-values` | allowed-values | `hash` | it is declared on the flag algorithm, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-back-matter-resource-prop-name-values` | allowed-values | `resource` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-back-matter-resource-prop-type-values` | allowed-values | `resource` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-by-component-link-rel-values` | allowed-values | `by-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-by-component-responsible-role-id-values` | allowed-values | `by-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-catalog-metadata-link-rel-type` | allowed-values | `catalog` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-catalog-metadata-prop-name` | allowed-values | `catalog` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-characterization-facet-name-system-values` | allowed-values | `facet` | it is declared on the flag system, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-component-allows-authenticated-scan-value` | allowed-values | `defined-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-asset-type-value` | allowed-values | `defined-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-hardware-service-software-prop-name-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-implementation-point-value` | allowed-values | `defined-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-interconnection-link-rel-values` | allowed-values | `system-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-interconnection-prop-name-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-interconnection-responsible-role-id-values` | allowed-values | `system-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-interconnection-service-software-system-prop-name-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-inventory-item-allows-authenticated-scan-values` | allowed-values | `system-implementation` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-link-rel-type` | allowed-values | `defined-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-link-rel-values` | allowed-values | `system-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-prop-allows-authenticated-scan-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-asset-type-values` | allowed-values | `system-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-prop-direction-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-implementation-point-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-ipaddress-class-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-is-public-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-is-virtual-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-name` | allowed-values | `defined-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-name-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-prop-validation-link-rel-values` | allowed-values | `system-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-protocol-transport-values` | allowed-values | `port-range` | it is declared on the flag transport, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-component-public-value` | allowed-values | `defined-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-responsible-role-id-values` | allowed-values | `system-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-role-id` | allowed-values | `defined-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-service-link-rel-values` | allowed-values | `system-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-component-software-prop-name-values` | allowed-values | `system-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-component-status-state-values` | allowed-values | `status` | it is declared on the flag state, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-component-type` | allowed-values | `-` | it is declared on the flag defined-component-type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-component-type-values` | allowed-values | `-` | it is declared on the flag system-component-type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-component-virtual-value` | allowed-values | `defined-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-link-rel-type` | allowed-values | `control` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-control-objective-part-method-prop-value` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-objective-part-subpart-name` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-part-name` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-prop-name` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-prop-status-value` | allowed-values | `control` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-control-statement-part-name` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-statement-part-prop-name` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-statement-part-rmf-prop-name` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-control-statement-part-subpart-name` | allowed-values | `control` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-ac-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-at-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-au-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-av-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-e-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-env-cia-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-mac-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-mat-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-mav-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-mpr-mvs-cia-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-msc-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-msi-msa-cia-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-mui-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-pr-cia-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-r-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-re-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-s-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-u-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-ui-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-v-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-cvss-v4.0-vectors` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-diagram-link-rel-values` | allowed-values | `diagram` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-facet-cve-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-access-complexity-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-access-vector-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-authentication-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-cia-requirement-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-collateral-damage-potential-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-confidentiality-impact-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-exploitability-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-name-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-remediation-level-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss2-report-confidence-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-access-complexity-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-access-vector-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-cia-impact-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-cia-requirement-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-exploit-code-maturity-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-modified-attack-complexity-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-modified-attack-vector-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-modified-cia-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-modified-scope-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-modified-user-interaction-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-name-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-remediation-level` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-report-confidence-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-scope` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-cvss3-user-interaction` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-fedramp-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-name-core-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-prop-name-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-facet-prop-state-values` | allowed-values | `facet` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-finding-target-reason-values` | allowed-values | `status` | it is declared on the flag reason, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-finding-target-status-state-values` | allowed-values | `status` | it is declared on the flag state, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-finding-target-values` | allowed-values | `finding-target` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-fips-199-impact-levels` | allowed-values | `system-information` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-group-part-name` | allowed-values | `group` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-group-prop-name` | allowed-values | `group` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-implementation-status-values` | allowed-values | `implementation-status` | it is declared on the flag state, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-implemented-component-prop-name-values` | allowed-values | `implemented-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-implemented-component-responsible-party-role-id-values` | allowed-values | `implemented-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-implemented-requirement-responsible-role-id-values` | allowed-values | `implemented-requirement` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-implemented-requirement-statement-by-component-prop-control-origination-values` | allowed-values | `implemented-requirement` | its target names a value through an expression outside the subset this tool parses: (.|statement|.//by-component)/prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal') and @name='control-origination']/@value |
| `oscal-implemented-requirement-statement-by-component-prop-name-values` | allowed-values | `implemented-requirement` | its target names a value through an expression outside the subset this tool parses: (.|statement|.//by-component)/prop[has-oscal-namespace('http://csrc.nist.gov/ns/oscal')]/@name |
| `oscal-information-type-categorization-system-values` | allowed-values | `categorization` | it is declared on the flag system, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-inventory-item-hardware-service-software-prop-name-values` | allowed-values | `inventory-item` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-inventory-item-link-rel-values` | allowed-values | `inventory-item` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-inventory-item-prop-asset-type-values` | allowed-values | `inventory-item` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-inventory-item-prop-is-scanned-values` | allowed-values | `inventory-item` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-inventory-item-prop-name-values` | allowed-values | `inventory-item` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-inventory-item-responsible-party-role-id-values` | allowed-values | `inventory-item` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-leveraged-authorization-link-rel-values` | allowed-values | `leveraged-authorization` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-mapping-coverage-generation-method-values` | allowed-values | `coverage` | it is declared on the flag generation-method, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-metadata-action-system-values` | allowed-values | `action` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-metadata-action-type-values` | allowed-values | `action` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-metadata-address-location-type-values` | allowed-values | `-` | it is declared on the flag location-type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-metadata-document-id-scheme-values` | allowed-values | `document-id` | it is declared on the flag scheme, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-metadata-link-rel-values` | allowed-values | `link` | it is declared on the flag rel, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-metadata-link-rel-values` | allowed-values | `metadata` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-metadata-location-prop-name-values` | allowed-values | `location` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-metadata-location-prop-type-data-center-values` | allowed-values | `location` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-metadata-location-prop-type-values` | allowed-values | `location` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-metadata-party-external-id-values` | allowed-values | `external-id` | it is declared on the flag scheme, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-metadata-party-prop-name-values` | allowed-values | `party` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-metadata-party-type-values` | allowed-values | `party` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-metadata-prop-name-values` | allowed-values | `prop` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-metadata-prop-name-values` | allowed-values | `metadata` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-metadata-responsible-party-role-ids` | allowed-values | `metadata` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-metadata-revision-link-rel-types` | allowed-values | `revision` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-metadata-telephone-number-type-values` | allowed-values | `telephone-number` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-observation-method-type-values` | allowed-values | `method` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-observation-values` | allowed-values | `type` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-origin-actor-type-values` | allowed-values | `origin-actor` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-parameter-how-many-type` | allowed-values | `parameter-selection` | it is declared on the flag how-many, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-parameter-prop-name` | allowed-values | `param` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-part-prop-name` | allowed-values | `part` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-profile-alter-by-item-name-values` | allowed-values | `remove` | it is declared on the flag by-item-name, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-profile-insert-controls-order-values` | allowed-values | `insert-controls` | it is declared on the flag order, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-profile-merge-combine-method-values` | allowed-values | `combine` | it is declared on the flag method, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-profile-modify-alter-prop-name-values` | allowed-values | `add` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-response-lifecycle-values` | allowed-values | `response` | it is declared on the flag lifecycle, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-response-prop-name` | allowed-values | `response` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-response-prop-type-value` | allowed-values | `response` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-risk-prop-name-values` | allowed-values | `entry` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-risk-prop-name-values` | allowed-values | `risk` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-risk-prop-type-values` | allowed-values | `entry` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-risk-status-values` | allowed-values | `risk-status` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-rmf-parameter-prop-name` | allowed-values | `param` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-service-component-link-rel-type` | allowed-values | `defined-component` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-software-component-prop-name` | allowed-values | `defined-component` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-statement-responsible-role-id-values` | allowed-values | `statement` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-status-state-values` | allowed-values | `status` | it is declared on the flag state, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-system-characteristics-prop-cloud-deployment-model-values` | allowed-values | `system-characteristics` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-system-characteristics-prop-cloud-service-model-values` | allowed-values | `system-characteristics` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-system-characteristics-prop-name-values` | allowed-values | `system-characteristics` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-system-characteristics-prop-name-values` | allowed-values | `system-characteristics` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-system-characteristics-prop-sp-800-63-assurance-level-values` | allowed-values | `system-characteristics` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-system-characteristics-responsible-party-role-id-values` | allowed-values | `system-characteristics` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-system-identifier-type-values` | allowed-values | `system-id` | it is declared on the flag identifier-type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-system-information-link-rel-values` | allowed-values | `system-information` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-system-information-prop-name-values` | allowed-values | `system-information` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-system-information-prop-privacy-designation-values` | allowed-values | `system-information` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-task-values` | allowed-values | `task` | it is declared on the flag type, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-terms-and-conditions-part-name` | allowed-values | `terms-and-conditions` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-threat-id-system` | allowed-values | `threat-id` | it is declared on the flag system, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-timing-unit-values` | allowed-values | `at-frequency` | it is declared on the flag unit, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-user-prop-name-values` | allowed-values | `system-user` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-user-prop-privilege-level-values` | allowed-values | `system-user` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-user-prop-type-values` | allowed-values | `system-user` | its value set is closed, since allow-other defaults to "no" where it is not declared, and this tool does not resolve the applicable set of constraints sharing its target, which is what decides the permitted values |
| `oscal-user-role-id-values` | allowed-values | `system-user` | it declares allow-other="yes", which opens its value set only if every other allowed-values constraint sharing its target does too, and this tool does not resolve that applicable set |
| `oscal-with-child-controls-values` | allowed-values | `-` | it is declared on the flag with-child-controls, so it constrains that flag's value wherever the flag is used, and this tool resolves constraints through the assembly and field names a target selects by, not through flag definitions |
| `oscal-back-matter-resource-citation-title` | expect | `resource` | its test is a Metapath expression this tool does not implement: title (on resource) |
| `oscal-catalog-control-require-statement-when-appropriate` | expect | `control` | its test is a Metapath expression this tool does not implement: prop[@name='status']/@value=('withdrawn','Withdrawn', 'reserved', 'superseded') or part[@name='statement'] (on control) |
| `oscal-component-protocol-port-range-has-end` | expect | `port-range` | its test is a Metapath expression this tool does not implement: exists(@end) (on port-range) |
| `oscal-component-protocol-port-range-has-start` | expect | `port-range` | its test is a Metapath expression this tool does not implement: exists(@start) (on port-range) |
| `oscal-component-protocol-port-range-starts-before-end` | expect | `port-range` | its test is a Metapath expression this tool does not implement: not(@start > @end) (on port-range) |
| `oscal-component-protocol-uuid` | expect | `protocol` | its test is a Metapath expression this tool does not implement: @uuid (on protocol) |
| `oscal-information-type-uuid` | expect | `information-type` | its test is a Metapath expression this tool does not implement: @uuid (on information-type) |
| `oscal-metadata-link-uri-reference-no-media-type` | expect | `link` | its test is a Metapath expression this tool does not implement: not(exists(@media-type)) (on link) |
| `oscal-method-part-has-method-prop` | expect | `control` | its test is a Metapath expression this tool does not implement: prop[has-oscal-namespace(('http://csrc.nist.gov/ns/oscal','http://csrc.nist.gov/ns/rmf')) and @name='method'] (on control) |
| `oscal-parameter-depends-on-deprecated` | expect | `param` | its test is a Metapath expression this tool does not implement: not(exists(@depends-on)) (on param) |
| `oscal-poam-item-uuid` | expect | `poam-item` | its test is a Metapath expression this tool does not implement: @uuid (on poam-item) |
| `oscal-profile-req-merge-combine` | expect | `combine` | its test is a Metapath expression this tool does not implement: not(@method='merge') (on combine) |
| `oscal-ssp-by-component-uuid-index` | index | `system-security-plan` | its target dereferences a second document through doc(), which this tool does not implement: control-implementation/implemented-requirement//by-component|doc(system-implementation/leveraged-authorization/link[@rel='system-security-plan']/@href)/system-security-plan/control-implementation/implemented-requirement//by-component |
| `oscal-component-prop-ipv4address-value-datatype` | matches | `system-component` | the vendored JSON Schema declares no datatype named ip-v4-address, so there is no published pattern to apply and writing one would be a rule encoded from memory |
| `oscal-component-prop-ipv6address-value-datatype` | matches | `system-component` | the vendored JSON Schema declares no datatype named ip-v6-address, so there is no published pattern to apply and writing one would be a rule encoded from memory |
| `oscal-component-prop-isa-date-value-datatype` | matches | `system-component` | the vendored JSON Schema declares no datatype named date-time, so there is no published pattern to apply and writing one would be a rule encoded from memory |
| `oscal-diagram-datatype-uri` | matches | `diagram` | its target names a value through an expression outside the subset this tool parses: link[@rel='diagram']/@href[not(starts-with(.,'#'))] |
| `oscal-diagram-datatype-uri-reference` | matches | `diagram` | its target names a value through an expression outside the subset this tool parses: link[@rel='diagram']/@href[starts-with(.,'#')] |
| `oscal-leveraged-authorization-link-rel-ssp-datatype-uri` | matches | `leveraged-authorization` | its target names a value through an expression outside the subset this tool parses: link[@rel='system-security-plan']/@href[not(starts-with(.,'#'))] |
| `oscal-leveraged-authorization-link-rel-ssp-datatype-uri-reference` | matches | `leveraged-authorization` | its target names a value through an expression outside the subset this tool parses: link[@rel='system-security-plan']/@href[starts-with(.,'#')] |
| `oscal-metadata-link-reference-href-datatype-uri` | matches | `link` | its target names a value through an expression outside the subset this tool parses: .[@rel=('reference') and not(starts-with(@href,'#'))]/@href |
| `oscal-metadata-link-reference-href-datatype-uri-reference` | matches | `link` | the vendored JSON Schema declares uri-reference without a pattern, as 'A URI Reference, either a URI or a relative-reference, formatted according to section 4.1 of RFC3986.', so it carries nothing this check can apply to a value |
| `oscal-metadata-link-resource-fragment-datatype` | matches | `link` | its regex (?:[0-9a-zA-Z-._~/?!$&'()*+,;=:@]|%[0-9A-F][0-9A-F])+ is not anchored at both ends, and the Metaschema specification does not say which regular-expression dialect a matches constraint uses. The datatypes page names two that disagree here, ECMA-262 as JSON Schema uses it, which searches, and XML Schema's, which anchors, and choosing one would be deciding something no published text decides |
| `oscal-metadata-location-address-country-regex` | matches | `country` | its regex [A-Z]{2} is not anchored at both ends, and the Metaschema specification does not say which regular-expression dialect a matches constraint uses. The datatypes page names two that disagree here, ECMA-262 as JSON Schema uses it, which searches, and XML Schema's, which anchors, and choosing one would be deciding something no published text decides |
| `oscal-risk-priority-datatype` | matches | `risk` | the vendored JSON Schema declares integer without a pattern, as 'A whole number value.', so it carries nothing this check can apply to a value |
| `oscal-system-information-pia-datatype-uri` | matches | `system-information` | its target names a value through an expression outside the subset this tool parses: link[@rel='privacy-impact-assessment']/@href[not(starts-with(.,'#'))] |
| `oscal-system-information-pia-datatype-uri-reference` | matches | `system-information` | its target names a value through an expression outside the subset this tool parses: link[@rel='privacy-impact-assessment']/@href[starts-with(.,'#')] |
